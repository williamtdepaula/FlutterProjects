import 'package:flutter/material.dart';

class Hello_page1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Page 1'),
        centerTitle: true,
        backgroundColor: Colors.blue,
      ),
    );
  }
}
