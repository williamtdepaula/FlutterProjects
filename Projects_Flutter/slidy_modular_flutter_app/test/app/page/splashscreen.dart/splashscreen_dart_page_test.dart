import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:slidy_modular_flutter_app/app/page/splashscreen.dart/splashscreen_dart_page.dart';

void main() {
  testWidgets('SplashscreenPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SplashscreenPage(title: 'Splashscreen')));
    //  final titleFinder = find.text('Splashscreen');
    //  expect(titleFinder, findsOneWidget);
  });
}
